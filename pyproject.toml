[project]
name = "osrs-prices"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Matt Flowers", email = "mattmflowers28@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
osrs-prices = "osrs_prices:main"

[build-system]
requires = ["uv_build>=0.9.5,<0.10.0"]
build-backend = "uv_build"
